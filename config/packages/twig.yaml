twig:
    file_name_pattern: '*.twig'
    form_themes: ['bootstrap_5_layout.html.twig']
    # Les heures affichées au destinataire sont les siennes, pas celles du serveur.
    date:
        timezone: Europe/Paris

when@test:
    twig:
        strict_variables: true
