framework:
    validation:
        # Enables validator auto-mapping support.
        # For instance, basic validation constraints will be inferred from Doctrine's metadata.
        #auto_mapping:
        #    App\Entity\: []

# La vérification interroge haveibeenpwned : inutile hors production, et
# bloquante quand la machine est hors ligne.
when@dev:
    framework:
        validation:
            not_compromised_password: false

when@test:
    framework:
        validation:
            not_compromised_password: false
